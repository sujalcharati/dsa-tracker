NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Shared login password — both Sujal and Pratyush type this on /login
# before they can click their name. Pick any string; share it via Signal.
# Server-only (not NEXT_PUBLIC_), never reaches the browser.
SHARED_PASSWORD=change-me-before-deploy
